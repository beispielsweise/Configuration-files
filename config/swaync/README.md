## Setting up swaync configuration
By defualt config and style may or may not be defined.
To properly use swaync, run the following commands in swaync directory:

ln -s configs/<configname>.json config.json
ln -s styles/<stylename> style.css
