// Colors, border widths, default values for button paddings, roundings, etc. is stored here
// Maybe divided in classes?
// Monochrome theme, inside var colors, var metrics, etc
var colors = {
    background: "#0F1419",
    backgroundPassive: "#131824",
    backgroundActive: "#202636",
    textActive: "#e6e6e6",
    textPassive: "#8f95a3"
}

var metrics = {
    radius: 14,
    gapIn: 0,
    gapOut: 0
}
