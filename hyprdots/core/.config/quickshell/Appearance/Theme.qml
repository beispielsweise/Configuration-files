// portential theme loader, only properties here?
// get all the values into the system (globalstate?)

import Quickshell
import QtQuick
import "Monochrome.js" as ActiveTheme

QtObject {
    readonly property var c: Theme.colors
    readonly property var m: Theme.metrics
}
